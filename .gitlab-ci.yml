include:
  - project: "signageos/infra/gitlab-ci"
    file: "/pipelines/ts-lib-public.yaml"
    ref: master

build:build-npm-package:
  stage: build
  script:
    - echo "No npm build needed for codestyle library - using source files directly"

test:general:
  stage: test
  script:
    - echo "No general tests needed for codestyle library"

test:types:
  stage: test
  script:
    - echo "No type checking needed for codestyle library"
